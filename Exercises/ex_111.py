#This is the same code as 111, because the 107-110 should be using modules and not package,
# so 111 is just the same code.
from modules import ex_108

number = float(input('Enter a value: '))
ex_108.get(number, 2, 4)
